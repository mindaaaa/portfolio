/**
 * ===========================================
 * 📌 main.js
 * - 여러 개의 모듈을 한 번에 import하여 앱을 초기화하는 역할
 * - 모듈 목록: scroll.js,
 * - `defer` 속성으로 HTML 파싱 후 실행
 * ===========================================
 */
